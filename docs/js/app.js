const scrollContainer = document.getElementById("scroll-container");
const navButtons = Array.from(document.querySelectorAll("[data-page]"));

const STORAGE_KEY = "rag-ai-scroll-state";
const TRANSITION_DURATION = 600;

const pageData = {
  home: {
    steps: [
      { key: "hero", eyebrow: "Welcome", title: "RAG AI Document Assistant", copy: "A system that turns static documents into instant, grounded answers by pairing retrieval with generation.", detail: "The experience starts with a clear product story: documents stay readable, answers stay traceable, and the interface stays focused on the workflow.", stickyTitle: "Instant grounded answers", stickyCopy: "This assistant helps people move from document overload to reliable answers without losing the source trail.", tags: ["Retrieval", "Generation", "Grounded"] },
      { key: "problem", eyebrow: "The problem", title: "Documents are slow to work through", copy: "Important details get buried in long files, search takes too long, and traditional AI can sound confident without being reliable.", detail: "When information is scattered across pages, the real cost is time spent hunting for the right passage instead of using it.", stickyTitle: "Hidden information", stickyCopy: "The system exists because reading every page manually is slow and error-prone.", tags: ["Slow", "Buried", "Risk"] },
      { key: "solution", eyebrow: "Why it matters", title: "Grounding reduces wasted time and bad calls", copy: "A grounded workflow cuts down search time, lowers decision delays, and reduces the risk of acting on incorrect answers.", detail: "The point is not more AI for its own sake. The point is faster understanding with better confidence.", stickyTitle: "Practical trust", stickyCopy: "The assistant is useful because it makes answers easier to verify.", tags: ["Faster", "Safer", "Clearer"] },
      { key: "pipeline", eyebrow: "How it works", title: "Retrieval and generation work together", copy: "The system finds relevant passages first, then generates an answer from those passages so the response stays tied to the source data.", detail: "That is the core difference between a grounded assistant and a generic chatbot.", stickyTitle: "RAG flow", stickyCopy: "The answer comes from evidence, not guesswork.", tags: ["Retrieve", "Generate", "Evidence"] },
      { key: "answer", eyebrow: "Value", title: "Your data stays explainable and useful", copy: "Answers come from your documents, the reasoning is easier to trace, and the output stays verifiable instead of opaque.", detail: "That makes the assistant a practical tool for work, study, and research instead of just a demo.", stickyTitle: "Explainable outputs", stickyCopy: "Users can trust the answer because they can follow the supporting context.", tags: ["Verifiable", "Traceable", "Useful"] }
    ]
  },
  technical: {
    steps: [
      { key: "overview", eyebrow: "System overview", title: "RAG needs retrieval before generation", copy: "The technical page explains why the app does not ask the model to answer from memory alone: relevant context has to be found first.", detail: "That keeps the system aligned with the source material instead of relying on unsupported output.", stickyTitle: "Retrieval first", stickyCopy: "The assistant gathers context before it generates a response.", tags: ["RAG", "Retrieval", "Grounding"] },
      { key: "ingestion", eyebrow: "Ingestion", title: "The app accepts PDFs and text files", copy: "Uploaded files enter the existing preprocessing pipeline so they can be prepared for analysis without changing the backend contract.", detail: "The goal is simple: bring documents in, keep the source data intact, and make it ready for the next stage.", stickyTitle: "Prepare data", stickyCopy: "Files are accepted and prepared for downstream analysis.", tags: ["PDFs", "Text", "Preprocessing"] },
      { key: "chunking", eyebrow: "Chunking", title: "Documents are split into smaller segments", copy: "The processing flow breaks documents into chunks so each segment can carry useful context without becoming too large.", detail: "Overlap helps maintain continuity across boundaries when a thought spans more than one chunk.", stickyTitle: "Context preservation", stickyCopy: "Chunking keeps the meaning readable in smaller pieces.", tags: ["Segments", "Overlap", "Context"] },
      { key: "embeddings", eyebrow: "Embeddings", title: "Text is converted into vectors", copy: "Embedding the chunks gives the system a semantic representation it can compare by meaning instead of only by exact wording.", detail: "That is what makes the retrieval step effective for similar ideas, not just identical phrases.", stickyTitle: "Semantic representation", stickyCopy: "Vectors capture meaning well enough for search to work by similarity.", tags: ["Vectors", "Similarity", "Meaning"] },
      { key: "retrieval", eyebrow: "Vector storage", title: "Stored embeddings support fast similarity search", copy: "Once vectors are available, the system can compare the query against stored passages and surface the most relevant matches quickly.", detail: "This is the search layer that connects the user’s question to the document evidence.", stickyTitle: "Fast search", stickyCopy: "The best passages are selected from stored embeddings.", tags: ["Storage", "Search", "Matches"] },
      { key: "response", eyebrow: "Generation", title: "The answer is generated from retrieved evidence", copy: "After the relevant chunks are found, the model uses that evidence to produce the response and expose the supporting passages.", detail: "That keeps the output readable, grounded, and easier to verify after the fact.", stickyTitle: "Evidence-based output", stickyCopy: "The response comes from retrieved context and can be traced back to sources.", tags: ["Answer", "Sources", "Traceable"] }
    ]
  },
  workspace: {
    introTitle: "Upload documents and ask grounded questions",
    introCopy: "The backend remains unchanged. This panel only talks to the existing ingestion, query, and reset routes.",
    steps: []
  }
};

const state = {
  page: resolvePageFromLocation(),
  activeStep: 0,
  renderToken: 0,
  scrollMemory: loadScrollMemory(),
  scrollListenerAttached: false,
  controlsBound: false,
  visitedPages: {},
  isTransitioning: false,
  transitionTimer: null
};

function escapeHtml(value) {
  return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
}
function clamp(value, min, max) { return Math.min(Math.max(value, min), max); }
function getHeaderOffset() {
  const headerHeight = Number.parseFloat(getComputedStyle(document.documentElement).getPropertyValue("--header-height"));
  return Number.isFinite(headerHeight) ? headerHeight : 132;
}
function loadScrollMemory() { try { const raw = window.localStorage.getItem(STORAGE_KEY); return raw ? JSON.parse(raw) : {}; } catch { return {}; } }
function saveScrollMemory() { try { window.localStorage.setItem(STORAGE_KEY, JSON.stringify(state.scrollMemory)); } catch {} }
function resolvePageFromLocation() { const path = window.location.pathname.replace(/\/+$/, "") || "/"; if (path === "/technical" || path === "/technical.html") return "technical"; if (path === "/workspace" || path === "/workspace.html") return "workspace"; if (path === "/index.html") return "home"; return "home"; }
function useStaticHtmlRouting() { const path = window.location.pathname.replace(/\/+$/, "") || "/"; return path === "/" || path.endsWith(".html"); }
function pagePath(pageName) { if (useStaticHtmlRouting()) { if (pageName === "technical") return "technical.html"; if (pageName === "workspace") return "workspace.html"; return "index.html"; } if (pageName === "technical") return "/technical"; if (pageName === "workspace") return "/workspace"; return "/"; }
function syncNavState(pageName) { navButtons.forEach((button) => button.classList.toggle("is-active", button.dataset.page === pageName)); }
function getSteps(pageName) { return pageData[pageName]?.steps || []; }
function getStep(pageName, index) { return getSteps(pageName)[index] || null; }
function renderTagList(tags = []) { return tags.map((tag) => `<span class="tag-chip">${escapeHtml(tag)}</span>`).join(""); }
function buildStepTriggers(pageName) { return getSteps(pageName).map((step, index) => `<section class="step-trigger" data-step="${index}" aria-hidden="true"><span class="visually-hidden">${escapeHtml(step.eyebrow)} ${escapeHtml(step.title)}</span></section>`).join(""); }
function buildFlowPanel(pageName, step, activeIndex) { const steps = getSteps(pageName); return `<section class="flow-panel"><p class="flow-panel__eyebrow">${pageName === "home" ? "Story rail" : "Engineering rail"}</p><div class="flow-panel__frame"><p class="flow-panel__label">Current step</p><h4>${escapeHtml(step.stickyTitle)}</h4><p>${escapeHtml(step.stickyCopy)}</p></div><div class="flow-panel__dots" aria-label="Step rail">${steps.map((item, index) => `<button class="flow-chip ${index === activeIndex ? "is-active" : ""}" type="button" data-step-nav="${index}" aria-label="Jump to ${escapeHtml(item.title)}">${escapeHtml(item.title)}</button>`).join("")}</div></section>`; }
function buildSlideMarkup(pageName, step, activeIndex) { return `<div class="slide-shell"><section class="slide-copy"><p class="slide-kicker">${escapeHtml(step.eyebrow)}</p><h2>${escapeHtml(step.title)}</h2><p class="slide-lead">${escapeHtml(step.copy)}</p><p class="slide-detail">${escapeHtml(step.detail)}</p><div class="tag-row">${renderTagList(step.tags || [])}</div><div class="slide-meta"><span>Step: ${escapeHtml(step.title)}</span><span>${pageName === "home" ? "Home narrative" : "Technical narrative"}</span></div></section><aside class="slide-visual">${buildFlowPanel(pageName, step, activeIndex)}</aside></div>`; }
function buildWorkspaceShell() { const page = pageData.workspace; return `<section class="page workspace-page" data-page="workspace"><header class="workspace-header"><p class="page-kicker">Workspace</p><h2>${escapeHtml(page.introTitle)}</h2><p>${escapeHtml(page.introCopy)}</p></header><div class="workspace-shell"><section class="workspace-panel workspace-upload"><h4>Upload documents</h4><form class="workspace-form" id="workspace-upload-form"><input class="workspace-file" type="file" id="workspace-files" multiple accept=".pdf,.txt" /><div class="workspace-actions"><button class="secondary-button" type="button" id="workspace-reset">Reset index</button></div></form><p class="workspace-hint">Select files to upload automatically.</p></section><section class="workspace-panel workspace-chat"><h4>Ask a question</h4><div class="workspace-feed" id="workspace-feed" aria-live="polite"></div><form class="workspace-form" id="workspace-query-form"><textarea class="workspace-input" id="workspace-question" placeholder="Ask about your documents..."></textarea><div class="workspace-actions"><button class="primary-button" type="submit">Ask</button></div></form></section><aside class="workspace-spacer" aria-hidden="true"></aside></div></section>`; }
function buildPageShell(pageName) { if (pageName === "workspace") return buildWorkspaceShell(); const steps = getSteps(pageName); return `<section class="page page--${pageName}" data-page="${pageName}" style="--step-count:${steps.length};"><div class="scrolly-shell"><aside class="sticky-stage" id="sticky-stage" aria-live="polite"><div class="sticky-stage__inner" id="sticky-stage-inner"></div></aside><div class="step-track" aria-hidden="true">${buildStepTriggers(pageName)}</div></div></section>`; }
function renderSticky(step, pageName, instant = false) { const sticky = document.getElementById("sticky-stage-inner"); if (!sticky) return; const markup = buildSlideMarkup(pageName, step, state.activeStep); const token = ++state.renderToken; if (instant) { sticky.innerHTML = markup; return; } sticky.classList.add("is-transitioning"); window.setTimeout(() => { if (token !== state.renderToken) return; sticky.innerHTML = markup; window.requestAnimationFrame(() => sticky.classList.remove("is-transitioning")); }, 110); }
function setActiveSections(index) { Array.from(scrollContainer.querySelectorAll("[data-step]")).forEach((section) => section.classList.toggle("is-active", Number(section.dataset.step) === index)); }
function scrollToStep(index) { const target = scrollContainer.querySelector(`[data-step="${index}"]`); if (!target) return; const targetTop = Math.max(0, target.offsetTop - getHeaderOffset()); scrollContainer.scrollTop = targetTop; }
function releaseTransitionLock() { if (state.transitionTimer) window.clearTimeout(state.transitionTimer); state.transitionTimer = window.setTimeout(() => { state.isTransitioning = false; state.transitionTimer = null; }, TRANSITION_DURATION); }
function syncActiveStep(index, pageName, source) { const step = getStep(pageName, index); if (!step) return; const changed = pageName !== state.page || index !== state.activeStep; state.page = pageName; state.activeStep = index; document.documentElement.dataset.currentStep = String(index); setActiveSections(index); if (changed) renderSticky(step, pageName, source === "init"); }
function applyStep(index, { pageName = state.page, source = "scroll" } = {}) { if (source === "scroll" && state.isTransitioning) return; syncActiveStep(index, pageName, source); }
function getStepIndexFromScroll(pageName) { const steps = getSteps(pageName); if (!steps.length) return 0; const height = Math.max(scrollContainer.clientHeight, 1); return clamp(Math.round(scrollContainer.scrollTop / height), 0, steps.length - 1); }
function persistScroll() { if (state.page !== "workspace") return; state.scrollMemory[state.page] = scrollContainer.scrollTop; saveScrollMemory(); }
function restoreScroll(pageName) { scrollContainer.scrollTop = Number(state.scrollMemory[pageName] || 0); }
function bindScrollTracking() { if (state.scrollListenerAttached) return; scrollContainer.addEventListener("scroll", () => { persistScroll(); if (state.page === "workspace" || state.isTransitioning) return; applyStep(getStepIndexFromScroll(state.page), { pageName: state.page, source: "scroll" }); }, { passive: true }); state.scrollListenerAttached = true; }
function goToStep(stepIndex) { if (state.isTransitioning) return; const nextStep = getStep(state.page, stepIndex); if (!nextStep) return; state.isTransitioning = true; syncActiveStep(stepIndex, state.page, "button"); scrollToStep(stepIndex); releaseTransitionLock(); }
function bindStepControls() { if (state.controlsBound) return; scrollContainer.addEventListener("click", (event) => { const button = event.target.closest("[data-step-nav]"); if (!button || button.disabled) return; const nextIndex = Number(button.dataset.stepNav); if (Number.isNaN(nextIndex)) return; goToStep(nextIndex); }); state.controlsBound = true; }
function renderWorkspaceFeed(message, role = "assistant") { const feed = document.getElementById("workspace-feed"); if (!feed) return; const node = document.createElement("article"); node.className = "feed-message"; node.dataset.role = role; node.innerHTML = `<strong>${role === "user" ? "You" : "Assistant"}</strong><p>${escapeHtml(message)}</p>`; feed.appendChild(node); feed.scrollTop = feed.scrollHeight; }
function renderSources(sources = []) { if (!sources.length) return; const feed = document.getElementById("workspace-feed"); if (!feed) return; const assistantMessages = Array.from(feed.querySelectorAll('.feed-message[data-role="assistant"]')); const target = assistantMessages[assistantMessages.length - 1]; if (!target) return; const existing = target.querySelector(".source-dropdown"); if (existing) existing.remove(); const details = document.createElement("details"); details.className = "source-dropdown"; details.innerHTML = `<summary>Sources</summary><ol class="source-list">${sources.map((item) => `<li>${escapeHtml(item.text || "")}</li>`).join("")}</ol>`; target.appendChild(details); }
async function uploadDocuments(files) { const results = []; for (const file of files) { const formData = new FormData(); formData.append("file", file); const response = await fetch("/upload", { method: "POST", body: formData }); if (!response.ok) { const text = await response.text(); throw new Error(text || `Upload failed for ${file.name}`); } results.push(await response.json()); } return results; }
async function resetIndex() { const response = await fetch("/reset", { method: "POST" }); if (!response.ok) throw new Error("Reset failed"); }
async function askQuestion(question) { const response = await fetch("/query", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ question, top_k: 4 }) }); if (!response.ok) throw new Error("Query failed"); return response.json(); }
function bindWorkspace() { const uploadForm = document.getElementById("workspace-upload-form"); const queryForm = document.getElementById("workspace-query-form"); const resetButton = document.getElementById("workspace-reset"); const questionInput = document.getElementById("workspace-question"); const fileInput = document.getElementById("workspace-files"); let uploadInFlight = false; async function handleFileUpload() { const files = Array.from(fileInput.files || []); if (!files.length || uploadInFlight) return; uploadInFlight = true; renderWorkspaceFeed(`Uploading ${files.length} file(s)...`, "assistant"); try { const responses = await uploadDocuments(files); const chunkCount = responses.reduce((total, item) => total + Number(item.chunks_added || 0), 0); renderWorkspaceFeed(`Indexed ${chunkCount} chunks.`, "assistant"); fileInput.value = ""; } catch (error) { renderWorkspaceFeed(error.message, "assistant"); } finally { uploadInFlight = false; } } if (uploadForm) uploadForm.addEventListener("change", (event) => { if (event.target === fileInput) handleFileUpload(); }); if (queryForm) queryForm.addEventListener("submit", async (event) => { event.preventDefault(); const question = questionInput.value.trim(); if (!question) return; renderWorkspaceFeed(question, "user"); questionInput.value = ""; try { const data = await askQuestion(question); renderWorkspaceFeed(data.answer || "No answer returned.", "assistant"); renderSources(data.sources || []); } catch (error) { renderWorkspaceFeed(error.message, "assistant"); } }); if (resetButton) resetButton.addEventListener("click", async () => { try { await resetIndex(); const feed = document.getElementById("workspace-feed"); if (feed) feed.innerHTML = ""; renderWorkspaceFeed("Index cleared.", "assistant"); } catch (error) { renderWorkspaceFeed(error.message, "assistant"); } }); }
function showPage(pageName, pushState) { const nextPage = pageData[pageName] ? pageName : "home"; state.page = nextPage; syncNavState(nextPage); if (pushState) history.pushState({ page: nextPage }, "", pagePath(nextPage)); const shouldRestorePage = Boolean(state.visitedPages[nextPage]); scrollContainer.innerHTML = buildPageShell(nextPage); scrollContainer.scrollTop = 0; bindScrollTracking(); bindStepControls(); if (nextPage === "workspace") { bindWorkspace(); state.visitedPages.workspace = true; if (shouldRestorePage) restoreScroll(nextPage); else scrollContainer.scrollTop = 0; return; } state.visitedPages[nextPage] = true; state.activeStep = 0; document.documentElement.dataset.currentStep = "0"; renderSticky(getStep(nextPage, 0), nextPage, true); if (shouldRestorePage) { restoreScroll(nextPage); applyStep(getStepIndexFromScroll(nextPage), { pageName: nextPage, source: "scroll" }); } else { applyStep(0, { pageName: nextPage, source: "init" }); } }
function bindNavigation() { navButtons.forEach((button) => button.addEventListener("click", (event) => { event.preventDefault(); showPage(button.dataset.page, true); })); window.addEventListener("popstate", () => showPage(resolvePageFromLocation(), false)); }
function init() { syncNavState(state.page); bindNavigation(); showPage(state.page, false); }
init();
